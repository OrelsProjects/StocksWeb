export default class Purchase {
  constructor(id, ticker, date, amount, price) {
    this.id = id;
    this.ticker = ticker;
    this.date = date;
    this.amount = amount;
    this.price = price;
  }
}
